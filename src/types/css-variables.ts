export enum ECSSVariableFields {
  primary = '--primary',
  differencePrimary = '--difference-primary',
  bgPrimary = '--bg-primary',
  bgText = '--bg-text',
  grey1 = '--grey-1',
  grey2 = '--grey-2',
}
